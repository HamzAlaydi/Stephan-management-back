name: Deploy to EC2

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: SSH and Deploy
        uses: appleboy/ssh-action@v1.2.0  # Use specific version instead of main
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /tmp/Stephan-management-back
            git pull origin main
            npm ci --production
            pm2 reload ecosystem.config.js
            sudo systemctl reload nginx
            echo "Deployment completed successfully
